#include <stdio.h>
#include <stdlib.h>

int fac(int f);


int main() {
    int f;
     
    printf("Ingrese el número a utilizar\n");
    scanf("%d", &f);
    
    int fac(n); 
    printf("%d\n", fac(f)); 
    return 0;
}

int fac(int f){
    if (f==0) return 1;
    else{
        return n*fac(n-1);
    }
}
